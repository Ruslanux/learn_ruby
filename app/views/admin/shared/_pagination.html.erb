<% if @total_pages > 1 %>
  <div class="mt-4 sm:mt-6 flex justify-center">
    <nav class="flex items-center space-x-1">
      <% if @page > 1 %>
        <%= link_to url_for(page: @page - 1, **local_assigns.fetch(:extra_params, {})), class: "px-2 sm:px-3 py-2 bg-white border border-gray-300 rounded hover:bg-gray-100 text-sm" do %>
          &larr; <span class="hidden sm:inline"><%= t('admin.previous') %></span>
        <% end %>
      <% end %>

      <span class="px-2 sm:px-3 py-2 text-gray-600 text-sm">
        <%= t('admin.page_of', page: @page, total: @total_pages) %>
      </span>

      <% if @page < @total_pages %>
        <%= link_to url_for(page: @page + 1, **local_assigns.fetch(:extra_params, {})), class: "px-2 sm:px-3 py-2 bg-white border border-gray-300 rounded hover:bg-gray-100 text-sm" do %>
          <span class="hidden sm:inline"><%= t('admin.next') %></span> &rarr;
        <% end %>
      <% end %>
    </nav>
  </div>
<% end %>
