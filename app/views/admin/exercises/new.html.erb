<% content_for(:page_title, t('admin.new_exercise')) %>

<div class="mb-4 sm:mb-6">
  <%= link_to "← #{t('admin.back_to')} #{t('admin.exercises')}", admin_exercises_path, class: "text-gray-600 hover:text-gray-800 text-sm sm:text-base" %>
</div>

<div class="bg-white rounded-lg shadow p-4 sm:p-6">
  <h3 class="text-base sm:text-lg font-semibold text-gray-800 mb-4 sm:mb-6"><%= t('admin.create_exercise') %></h3>
  <%= render "form" %>
</div>
