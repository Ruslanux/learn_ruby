<!DOCTYPE html>
<html lang="<%= I18n.locale %>">
  <head>
    <title><%= content_for(:title) || t('app_name') %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 min-h-screen">
    <nav class="bg-red-600 text-white shadow-lg" data-controller="mobile-menu">
      <div class="container mx-auto px-4">
        <div class="flex justify-between items-center h-16">
          <!-- Logo -->
          <%= link_to root_path, class: "flex items-center space-x-2 flex-shrink-0" do %>
            <span class="text-xl md:text-2xl font-bold">💎 <%= t('app_name') %></span>
          <% end %>

          <!-- Desktop Navigation -->
          <div class="hidden md:flex items-center space-x-6">
            <%= link_to t('nav.lessons'), lessons_path, class: "hover:text-red-200 transition font-medium" %>
            <%= link_to t('nav.leaderboard'), leaderboard_path, class: "hover:text-red-200 transition font-medium" %>
            <% if user_signed_in? && current_user.admin? %>
              <%= link_to t('nav.admin'), admin_root_path, class: "hover:text-red-200 transition font-medium" %>
            <% end %>
          </div>

          <!-- Desktop Right Side -->
          <div class="hidden md:flex items-center space-x-4">
            <!-- Language Switcher -->
            <div class="flex items-center space-x-1 text-sm">
              <% I18n.available_locales.each do |loc| %>
                <% if loc == I18n.locale %>
                  <span class="bg-red-700 px-2 py-1 rounded font-medium"><%= loc.upcase %></span>
                <% else %>
                  <%= link_to loc.to_s.upcase, switch_locale_path(lang: loc), data: { turbo: false }, class: "hover:text-red-200 px-2 py-1 transition" %>
                <% end %>
              <% end %>
            </div>

            <% if user_signed_in? %>
              <%= link_to profile_path, class: "flex items-center space-x-2 hover:text-red-200 transition" do %>
                <span class="text-sm">
                  <%= t('nav.level_short') %> <%= current_user.level %> |
                  <%= current_user.total_points %> <%= t('nav.points_short') %>
                  <% if current_user.current_streak > 0 %>
                    | 🔥 <%= current_user.current_streak %>
                  <% end %>
                </span>
                <span class="font-medium"><%= current_user.username %></span>
              <% end %>
              <%= button_to t('nav.sign_out'), destroy_user_session_path, method: :delete, class: "bg-red-700 hover:bg-red-800 px-4 py-2 rounded-lg transition" %>
            <% else %>
              <%= link_to t('nav.sign_in'), new_user_session_path, class: "hover:text-red-200 transition" %>
              <%= link_to t('nav.sign_up'), new_user_registration_path, class: "bg-white text-red-600 px-4 py-2 rounded-lg hover:bg-red-100 transition" %>
            <% end %>
          </div>

          <!-- Mobile Menu Button -->
          <button type="button" class="md:hidden p-2 rounded-lg hover:bg-red-700 transition" data-action="mobile-menu#toggle">
            <!-- Hamburger Icon -->
            <svg data-mobile-menu-target="openIcon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
            <!-- Close Icon -->
            <svg data-mobile-menu-target="closeIcon" class="hidden w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>

        <!-- Mobile Menu -->
        <div data-mobile-menu-target="menu" class="hidden md:hidden pb-4">
          <div class="flex flex-col space-y-2">
            <!-- Navigation Links -->
            <%= link_to t('nav.lessons'), lessons_path, class: "block px-3 py-2 rounded-lg hover:bg-red-700 transition" %>
            <%= link_to t('nav.leaderboard'), leaderboard_path, class: "block px-3 py-2 rounded-lg hover:bg-red-700 transition" %>
            <% if user_signed_in? && current_user.admin? %>
              <%= link_to t('nav.admin'), admin_root_path, class: "block px-3 py-2 rounded-lg hover:bg-red-700 transition" %>
            <% end %>

            <!-- Divider -->
            <div class="border-t border-red-500 my-2"></div>

            <!-- Language Switcher -->
            <div class="flex items-center space-x-2 px-3 py-2">
              <span class="text-red-200 text-sm"><%= t('nav.language', default: 'Language') %>:</span>
              <% I18n.available_locales.each do |loc| %>
                <% if loc == I18n.locale %>
                  <span class="bg-red-700 px-2 py-1 rounded text-sm font-medium"><%= loc.upcase %></span>
                <% else %>
                  <%= link_to loc.to_s.upcase, switch_locale_path(lang: loc), data: { turbo: false }, class: "hover:bg-red-700 px-2 py-1 rounded text-sm transition" %>
                <% end %>
              <% end %>
            </div>

            <!-- Divider -->
            <div class="border-t border-red-500 my-2"></div>

            <!-- User Section -->
            <% if user_signed_in? %>
              <%= link_to profile_path, class: "block px-3 py-2 rounded-lg hover:bg-red-700 transition" do %>
                <div class="font-medium"><%= current_user.username %></div>
                <div class="text-sm text-red-200">
                  <%= t('nav.level_short') %> <%= current_user.level %> |
                  <%= current_user.total_points %> <%= t('nav.points_short') %>
                  <% if current_user.current_streak > 0 %>
                    | 🔥 <%= current_user.current_streak %>
                  <% end %>
                </div>
              <% end %>
              <%= button_to t('nav.sign_out'), destroy_user_session_path, method: :delete, class: "w-full text-left px-3 py-2 rounded-lg hover:bg-red-700 transition" %>
            <% else %>
              <%= link_to t('nav.sign_in'), new_user_session_path, class: "block px-3 py-2 rounded-lg hover:bg-red-700 transition" %>
              <%= link_to t('nav.sign_up'), new_user_registration_path, class: "block px-3 py-2 bg-white text-red-600 rounded-lg hover:bg-red-100 transition text-center font-medium" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice %>
      <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative container mx-auto mt-4"
           role="alert"
           data-controller="flash"
           data-flash-delay-value="5000">
        <span class="block sm:inline pr-8"><%= notice %></span>
        <button type="button"
                class="absolute top-0 right-0 px-4 py-3 text-green-700 hover:text-green-900 transition"
                data-action="flash#dismiss"
                aria-label="Close">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative container mx-auto mt-4"
           role="alert"
           data-controller="flash"
           data-flash-delay-value="5000">
        <span class="block sm:inline pr-8"><%= alert %></span>
        <button type="button"
                class="absolute top-0 right-0 px-4 py-3 text-red-700 hover:text-red-900 transition"
                data-action="flash#dismiss"
                aria-label="Close">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
          </svg>
        </button>
      </div>
    <% end %>

    <main class="container mx-auto mt-8 px-4">
      <%= yield %>
    </main>

    <% unless content_for?(:hide_footer) %>
      <footer class="mt-auto py-6 text-center text-gray-500 text-sm">
        <p><%= t('app_name') %> - <%= t('tagline') %></p>
      </footer>
    <% end %>
  </body>
</html>
